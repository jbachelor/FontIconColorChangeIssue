<?xml version="1.0" encoding="UTF-8"?>
<ResourceDictionary
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:FontIconColorChanges.Styles"
    xmlns:projectRoot="clr-namespace:FontIconColorChanges"
    x:Class="FontIconColorChanges.Styles.DarkTheme">
    <ResourceDictionary.MergedDictionaries>
        <local:ThemeIndependentStyles />
    </ResourceDictionary.MergedDictionaries>
    <!-- Colors -->
    <Color
        x:Key="ThemeDependentTextColor">#FFFFFF</Color>

    <!-- Fonts -->
    <FontImageSource
        x:Key="ChevronRight"
        FontFamily="{StaticResource MaterialFontIcons}"
        Glyph="{x:Static projectRoot:MaterialFont.ChevronRight}"
        Size="{StaticResource MenuItemIconSize}"
        Color="{StaticResource LightTextColor}" />

    <!-- Implicit Styles -->
    <Style
        TargetType="Label">
        <Setter
            Property="TextColor"
            Value="#BBBBBB" />
    </Style>
</ResourceDictionary>
